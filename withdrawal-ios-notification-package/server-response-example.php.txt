<?php
// Example response contract only. Authenticate the user, validate the request,
// and deduct the withdrawal inside a database transaction before returning this.

header('Content-Type: application/json; charset=utf-8');

$withdrawalAmount = 50.00; // Replace with the validated, completed withdrawal amount.
$newDatabaseBalance = 350.00; // Read the user's real balance after the deduction.

echo json_encode([
    'success' => true,
    'message' => 'Withdrawal completed.',
    'amount' => $withdrawalAmount,
    'new_balance' => $newDatabaseBalance,
    'currency' => 'GHS',
], JSON_UNESCAPED_SLASHES);
